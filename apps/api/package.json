{
  "name": "@momento/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8790",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {"@momento/protocol": "0.1.0", "hono": "^4.9.8"},
  "devDependencies": {"@cloudflare/workers-types": "^4.20250906.0", "wrangler": "^4.36.0"}
}
